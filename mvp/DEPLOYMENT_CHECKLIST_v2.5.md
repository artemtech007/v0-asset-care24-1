# 📋 ЧЕК-ЛИСТ ВНЕДРЕНИЯ - AssetCare24 v2.5 "Система кандидатов"

**Версия:** v2.5
**Дата создания:** 26 января 2026 г.
**Ответственный:** Администратор системы
**Статус:** ⏳ ГОТОВ К ВНЕДРЕНИЮ

---

## 🎯 ОБЩАЯ ИНФОРМАЦИЯ

### **Что внедряется:**
- ✅ Система кандидатов на заявки
- ✅ Расширенный жизненный цикл заявок (11 статусов)
- ✅ Интеграция с Telegram ботом
- ✅ Обновленный админ-интерфейс

### **Ожидаемое время внедрения:** 2-3 часа
### **Риски:** Низкие (откат возможен)
### **Откат:** Полностью поддерживается

---

## ✅ ПРОВЕРКА ПРЕДВАРИТЕЛЬНЫХ УСЛОВИЙ

### **1. Доступы и окружение**
- [ ] Доступ к Supabase Dashboard
- [ ] Доступ к GitHub репозиторию
- [ ] Тестовое окружение доступно
- [ ] Резервная копия базы данных создана

### **2. Код готов**
- [x] Код запушен в ветку `master` (commit: `151a6e0`)
- [x] Тег `v2.5` создан
- [x] Миграционные скрипты готовы
- [x] Документация обновлена

### **3. Команда**
- [ ] Администратор системы доступен
- [ ] Разработчик доступен (при необходимости)
- [ ] План коммуникации с командой согласован

---

## 🔄 ЭТАП 1: МИГРАЦИЯ БАЗЫ ДАННЫХ (30 МИНУТ)

### **Подготовка:**
- [ ] Остановить все сервисы, использующие базу данных
- [ ] Создать резервную копию базы данных
- [ ] Открыть Supabase SQL Editor

### **Выполнение миграции:**

#### **Шаг 1.1: Создание таблицы кандидатов**
```sql
-- Выполнить: mvp/add_request_candidates_migration.sql (ОБНОВЛЕННАЯ ВЕРСИЯ)
```
- [ ] Скрипт выполнен без ошибок
- [ ] Проверить создание таблицы:
```sql
SELECT 'Таблица создана' as check_result,
       COUNT(*) as tables_count
FROM information_schema.tables
WHERE table_name = 'request_candidates';
```

#### **Шаг 1.2: Создание PostgreSQL функции**
```sql
-- Выполнить: mvp/create_assign_master_function.sql
```
- [ ] Функция создана без ошибок
- [ ] Проверить создание:
```sql
SELECT 'Функция создана' as check_result,
       proname as function_name
FROM pg_proc
WHERE proname = 'assign_master_to_request';
```

#### **Шаг 1.3: Проверка результатов миграции**
```sql
-- Выполнить проверочный запрос:
SELECT
    'Migration completed' as status,
    (SELECT COUNT(*) FROM request_candidates) as candidates_table,
    (SELECT COUNT(*) FROM requests WHERE status IN ('waiting_candidates', 'master_assigned')) as updated_requests,
    (SELECT COUNT(*) FROM pg_proc WHERE proname = 'assign_master_to_request') as functions_created;
```
- [ ] Все значения > 0
- [ ] Нет ошибок в логах

### **Откат (при необходимости):**
```sql
-- Удалить новую таблицу
DROP TABLE IF EXISTS request_candidates;

-- Удалить функцию
DROP FUNCTION IF EXISTS assign_master_to_request(bigint, text, text);

-- Восстановить статусы
UPDATE requests SET status = 'new' WHERE status IN ('waiting_candidates', 'candidates_collecting', 'master_selection');
UPDATE requests SET status = 'assigned' WHERE status = 'master_assigned';

-- Восстановить старый constraint
ALTER TABLE requests DROP CONSTRAINT IF EXISTS requests_status_check;
ALTER TABLE requests ADD CONSTRAINT requests_status_check
CHECK (status IN ('new', 'assigned', 'in_progress', 'completed', 'canceled', 'paused'));
```

### **Возможные проблемы и решения:**

#### **Ошибка: "violates check constraint requests_status_check"**
```
Причина: Constraint не позволяет новые статусы ('waiting_candidates', etc.)
Решение: Убедиться, что используется обновленная версия миграционного скрипта
         (commit: 6544826) с командами DROP/CREATE CONSTRAINT
```

#### **Ошибка: "column published_at does not exist"**
```
Причина: Скрипт миграции не выполнен полностью
Решение: Перезапустить миграцию с начала
```

#### **Ошибка: "function assign_master_to_request already exists"**
```
Причина: Функция уже создана в предыдущем запуске
Решение: DROP FUNCTION перед повторным запуском или использовать CREATE OR REPLACE
```

#### **Ошибка: "column m.rating does not exist"**
```
Причина: Поле rating отсутствует в таблице masters
Решение: Убедиться, что используется обновленная версия миграционного скрипта
         (commit: f97ea09) с добавлением поля rating в таблицу masters
```

#### **Ошибка: "cannot change name of view column"**
```
Причина: Конфликт имен колонок при обновлении view
Решение: Убедиться, что используется обновленная версия миграционного скрипта
         (commit: cfc5317) с правильными алиасами для published_at и telegram_message_id
```

---

## 🔄 ЭТАП 2: ЗАПУСК ОБНОВЛЕННОЙ АДМИНКИ (15 МИНУТ)

### **Подготовка:**
- [ ] Перейти в директорию проекта:
```bash
cd /home/aaa/Projects/n8n/assetcare24/site
```

### **Запуск:**
```bash
# Проверить порт 3000
npm run dev
# Если занят, использовать другой порт:
npm run dev -- --port 3001
```
- [ ] Сервер запустился без ошибок
- [ ] Нет ошибок компиляции TypeScript
- [ ] Админка доступна по адресу: `http://localhost:3001/dashboard/admin`

### **Проверка функциональности:**
- [ ] Загружается страница админки
- [ ] Видны все табы: Übersicht, Benutzer, Aufträge, Statistiken, Einstellungen
- [ ] В таб "Aufträge" отображаются заявки
- [ ] У заявок видны статусы и количество кандидатов

---

## 🔄 ЭТАП 3: ТЕСТИРОВАНИЕ ФУНКЦИОНАЛЬНОСТИ (45 МИНУТ)

### **Тест 3.1: Просмотр кандидатов**

**Сценарий:**
- [ ] Найти заявку со статусом "offen"
- [ ] Нажать кнопку "Kandidaten"
- [ ] Открывается модальное окно
- [ ] Виден список кандидатов с информацией о мастерах

**Ожидаемый результат:**
- [ ] Модальное окно открывается
- [ ] Показана информация о заявке
- [ ] Отображены кандидаты с рейтингом, специализацией, комментариями

### **Тест 3.2: Назначение мастера**

**Сценарий:**
- [ ] В модальном окне кандидатов нажать "Zuweisen" у кандидата
- [ ] Подтвердить назначение

**Ожидаемый результат:**
- [ ] Статус заявки меняется на "zugewiesen"
- [ ] Выбранный кандидат отмечен как "Ausgewählt"
- [ ] Остальные кандидаты отмечены как "Abgelehnt"
- [ ] Модальное окно закрывается

**Проверка в базе данных:**
```sql
-- Проверить обновление статусов
SELECT r.id, r.status, rc.master_id, rc.status as candidate_status
FROM requests r
LEFT JOIN request_candidates rc ON r.id = rc.request_id
WHERE r.id = [ID_ЗАЯВКИ];
```
- [ ] Статус заявки: `master_assigned`
- [ ] Один кандидат: `selected`
- [ ] Остальные кандидаты: `rejected`

### **Тест 3.3: Добавление тестового кандидата**

**Через API:**
```bash
curl -X POST http://localhost:3001/api/admin/orders/101/candidates \
  -H "Content-Type: application/json" \
  -d '{
    "master_id": "mid_wa_49987654321",
    "master_comment": "Тестовый кандидат для проверки"
  }'
```
- [ ] Запрос выполнен успешно (status: 200)
- [ ] Кандидат появился в списке

### **Тест 3.4: Проверка безопасности**

**Сценарий:**
- [ ] Попытаться назначить мастера на завершенную заявку
- [ ] Попытаться добавить кандидата дважды

**Ожидаемый результат:**
- [ ] Система корректно обрабатывает ошибки
- [ ] Возвращает понятные сообщения об ошибках

---

## 🔄 ЭТАП 4: ИНТЕГРАЦИЯ С TELEGRAM БОТОМ (60-120 МИНУТ)

### **Подготовка:**
- [ ] Доступ к n8n workflow
- [ ] Токены Telegram бота
- [ ] ID Telegram канала

### **Обновление workflow:**

#### **4.1: При создании заявки**
```
Добавить шаг: Обновление статуса при публикации
- status = 'waiting_candidates'
- published_at = NOW()
- telegram_message_id = ${TELEGRAM_MESSAGE_ID}
```

#### **4.2: При отклике мастера**
```
Добавить шаг: Добавление кандидата
INSERT INTO request_candidates (request_id, master_id, master_comment)
VALUES (${REQUEST_ID}, '${MASTER_ID}', '${MASTER_COMMENT}')

Если первый кандидат:
UPDATE requests SET status = 'candidates_collecting'
WHERE id = ${REQUEST_ID}
```

#### **4.3: При назначении мастера**
```
Добавить шаг: Обновление сообщения в Telegram
- Добавить информацию о назначенном мастере
- Изменить статус сообщения
```

### **Тестирование интеграции:**
- [ ] Создать тестовую заявку через бота
- [ ] Проверить публикацию в Telegram канал
- [ ] Имитировать отклик мастера
- [ ] Проверить появление кандидата в админке
- [ ] Назначить мастера и проверить обновление в Telegram

---

## 📊 ЭТАП 5: МОНИТОРИНГ И ОПТИМИЗАЦИЯ (30 МИНУТ)

### **Метрики производительности:**
```sql
-- Проверить скорость API
SELECT 'API Performance Check' as test,
       AVG(extract(epoch from (completed_at - started_at))) * 1000 as avg_response_ms
FROM pg_stat_statements
WHERE query LIKE '%api/admin/orders%';
```
- [ ] Среднее время ответа API < 500ms

### **Метрики бизнес-процессов:**
```sql
-- Проверить работу системы кандидатов
SELECT
    'Candidates System Health' as metric,
    COUNT(DISTINCT r.id) as total_requests,
    COUNT(rc.id) as total_candidates,
    COUNT(CASE WHEN r.status = 'master_assigned' THEN 1 END) as assigned_requests,
    ROUND(
        COUNT(CASE WHEN r.status = 'master_assigned' THEN 1 END)::decimal /
        NULLIF(COUNT(DISTINCT r.id), 0) * 100, 2
    ) as assignment_rate_percent
FROM requests r
LEFT JOIN request_candidates rc ON r.id = rc.request_id
WHERE r.created_at >= CURRENT_DATE - INTERVAL '7 days';
```
- [ ] Система работает корректно

### **Логи и мониторинг:**
- [ ] Проверить логи Next.js приложения (нет ошибок)
- [ ] Проверить логи Supabase (нет ошибок в запросах)
- [ ] Настроить алерты на ошибки в ключевых функциях

---

## 🎯 ЭТАП 6: ДОКУМЕНТИРОВАНИЕ И ПЕРЕДАЧА (30 МИНУТ)

### **Обновление документации:**
- [x] `mvp/03_database_schema_mvp.md` - обновлена до v2.9
- [x] `mvp/admin-dashboard-mapping.md` - карта админки
- [x] `mvp/database-schema-comparison.md` - сравнение схем
- [x] `mvp/request_candidates_implementation_plan.md` - план внедрения
- [ ] Обновить `mvp/README_MVP.md` с описанием новой функциональности

### **Создание инструкций для пользователей:**
- [ ] Инструкция для администраторов по работе с кандидатами
- [ ] Инструкция для мастеров по отклику на заявки
- [ ] Техническая документация для разработчиков

### **Передача знаний:**
- [ ] Провести демонстрацию новой функциональности
- [ ] Передать доступы и инструкции
- [ ] Определить процесс поддержки и обновлений

---

## 🚨 ПРОЦЕДУРА ОТКАТА (ПРИ КРИТИЧЕСКИХ ПРОБЛЕМАХ)

### **Полный откат к v2.4:**

#### **1. Откат базы данных:**
```sql
-- Удалить новую таблицу
DROP TABLE IF EXISTS request_candidates CASCADE;

-- Удалить функцию
DROP FUNCTION IF EXISTS assign_master_to_request(bigint, text, text);

-- Восстановить старые статусы
UPDATE requests SET
    status = CASE
        WHEN status IN ('waiting_candidates', 'candidates_collecting', 'master_selection') THEN 'new'
        WHEN status = 'master_assigned' THEN 'assigned'
        ELSE status
    END;

-- Удалить новые поля
ALTER TABLE requests DROP COLUMN IF EXISTS published_at;
ALTER TABLE requests DROP COLUMN IF EXISTS telegram_message_id;
```

#### **2. Откат кода:**
```bash
# Откат к предыдущему коммиту
git checkout [PREVIOUS_COMMIT_HASH]
npm install
npm run dev
```

### **3. Восстановление из резервной копии:**
- [ ] Восстановить базу данных из резервной копии
- [ ] Перезапустить сервисы
- [ ] Проверить работоспособность

---

## ✅ КРИТЕРИИ ГОТОВНОСТИ К ПРОДАКШЕНУ

### **Функциональные требования:**
- [ ] Все заявки отображаются корректно
- [ ] Кнопка "Kandidaten" работает для подходящих заявок
- [ ] Модальное окно кандидатов открывается
- [ ] Назначение мастера работает корректно
- [ ] Статусы обновляются правильно

### **Нефункциональные требования:**
- [ ] Время ответа API < 500ms
- [ ] Нет ошибок в логах
- [ ] UI работает на мобильных устройствах
- [ ] Безопасность данных обеспечена

### **Бизнес-требования:**
- [ ] Администратор может просматривать кандидатов
- [ ] Администратор может назначать мастеров
- [ ] Система устойчива к одновременному доступу
- [ ] Процесс прозрачен и отслеживаем

---

## 📞 КОНТАКТЫ И ПОДДЕРЖКА

### **В случае проблем:**
- **Технические проблемы:** [Контакт разработчика]
- **Проблемы с данными:** Создать issue в GitHub
- **Вопросы по бизнес-логике:** [Контакт администратора]

### **Мониторинг после внедрения:**
- Ежедневная проверка логов ошибок
- Мониторинг производительности API
- Отслеживание метрик использования системы кандидатов

---

## 🎉 ПОДТВЕРЖДЕНИЕ ВНЕДРЕНИЯ

**Внедрение завершено:** ____________________

**Дата:** ____________________

**Ответственный:** ____________________

**Результат:** ✅ УСПЕШНО / ⚠️ С ОШИБКАМИ / ❌ НЕУДАЧНО

**Комментарии:**
____________________________________________________________
____________________________________________________________
____________________________________________________________

---

**Документ создан:** 26 января 2026 г.
**Версия:** 1.0
**Статус:** ГОТОВ К ИСПОЛНЕНИЮ
