# Admin Dashboard - План реализации (AssetCare24 MVP)

**Дата создания:** 10 января 2026 г.
**Ответственный:** AI Assistant
**Статус:** Планирование

## 🎯 Цели и задачи

Реализовать функциональную админ-панель для управления платформой AssetCare24 с возможностью:
- Просмотра и управления пользователями (клиенты, мастера)
- Мониторинга заявок и их статуса
- Просмотра статистики и аналитики
- Управления контентом и настройками

## 🏗️ Архитектура данных

### Источники данных

#### 1. **Supabase** (основной источник)
- **Пользователи:** таблица `users`
- **Заявки:** таблица `requests`
- **Адреса:** таблица `user_addresses`
- **Медиа:** таблица `request_media`
- **Отзывы:** таблица `reviews`

#### 2. **n8n** (дополнительные данные)
- **Статус workflow:** информация о выполняющихся процессах
- **Webhook логи:** история отправленных уведомлений
- **Ошибка workflow:** логи сбоев в автоматизации

### API endpoints

#### Supabase REST API
```
GET  /rest/v1/users - список пользователей
GET  /rest/v1/requests - список заявок
GET  /rest/v1/user_addresses - адреса пользователей
GET  /rest/v1/request_media - медиа файлы
GET  /rest/v1/reviews - отзывы
POST /rest/v1/users - создание пользователя
PATCH /rest/v1/users/{id} - обновление пользователя
```

#### n8n REST API (опционально)
```
GET  /rest/executions - список выполнений workflow
GET  /rest/workflows - список workflow
GET  /rest/webhooks - webhook endpoints
```

## 🔧 Необходимая информация

### 1. **Supabase Credentials**
```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

### 2. **n8n API Credentials** (опционально)
```env
N8N_HOST=https://botstroicom.site
N8N_API_KEY=your-api-key  # если используется аутентификация
```

### 3. **База данных структура**
- Подтверждение актуальности схемы из `03_database_schema_mvp.md`
- Row Level Security (RLS) политики для админа
- Индексы для производительности

## 📊 Функциональные требования

### 1. **Панель управления пользователями**
- **Фильтры:** По роли (kunde/handwerker), статусу (aktiv/inaktiv/gesperrt)
- **Действия:** Просмотр деталей, блокировка/разблокировка, редактирование
- **Информация:** Контакты, статистика заказов, рейтинг (для мастеров)

### 2. **Мониторинг заявок**
- **Фильтры:** По статусу (new/assigned/completed/canceled), дате, мастеру
- **Действия:** Просмотр деталей, изменение статуса, назначение мастера
- **Информация:** Фото проблемы, описание, адрес, контакт клиента

### 3. **Статистика и аналитика**
- **Метрики:**
  - Общее количество пользователей/заявок
  - Конверсия заявок (new → completed)
  - Среднее время выполнения
  - Рейтинг мастеров
- **Графики:** По времени, по категориям услуг

### 4. **Управление контентом**
- **Настройки:** Тексты приветствий, категории услуг
- **Логи:** Просмотр ошибок системы, webhook ответов

## 🔄 Data Flow

### 1. **Загрузка данных**
```
Frontend (AdminDashboard)
    ↓ HTTP Requests
Supabase REST API
    ↓ SQL Queries
PostgreSQL Database
    ↓ Row Level Security
Filtered Data → Frontend
```

### 2. **Real-time обновления** (опционально)
```
Supabase Realtime
    ↓ WebSocket
Frontend
    ↓ Auto-refresh UI
```

## 🛠️ Техническая реализация

### Frontend (React/Next.js)

#### Хуки для данных:
```typescript
// Пользователи
const { data: users, loading: usersLoading } = useSupabaseQuery('users')

// Заявки с фильтрами
const { data: requests, loading: requestsLoading } = useSupabaseQuery('requests', {
  filters: { status: 'new', created_at: 'gte.2024-01-01' }
})

// Статистика
const { data: stats } = useSupabaseQuery('requests', {
  select: 'count,status',
  groupBy: 'status'
})
```

#### Компоненты:
- `UsersTable` - таблица пользователей с фильтрами
- `RequestsTable` - таблица заявок с действиями
- `StatsCards` - карточки с метриками
- `Charts` - графики аналитики

### Backend интеграция

#### Supabase Client:
```typescript
import { createClient } from '@supabase/supabase-js'

const supabase = createClient(
  process.env.SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY! // для админ доступа
)
```

#### API Routes (опционально):
```typescript
// /api/admin/users
export async function GET(request: Request) {
  const supabase = createRouteHandlerClient({ cookies })
  const { data, error } = await supabase
    .from('users')
    .select('*')
    .eq('role', 'admin') // RLS проверка

  return Response.json({ data, error })
}
```

## 🔐 Безопасность

### 1. **Аутентификация**
- JWT токены через Supabase Auth
- Проверка роли пользователя (role: 'admin')
- Session management

### 2. **Авторизация**
- Row Level Security (RLS) в Supabase
- Проверка разрешений на операции
- Аудит логов действий

### 3. **Валидация данных**
- Zod schemas для входящих данных
- Sanitization пользовательского ввода
- Rate limiting для API запросов

## 📋 План реализации

### Фаза 1: Базовая функциональность (1-2 дня)
1. **Настройка Supabase подключения**
2. **Реализация таблицы пользователей** с фильтрами
3. **Реализация таблицы заявок** с базовыми действиями

### Фаза 2: Статистика и аналитика (1 день)
1. **Метрики dashboard**
2. **Базовые графики**
3. **Экспорт данных**

### Фаза 3: Расширенные функции (2-3 дня)
1. **Управление пользователями** (блокировка/редактирование)
2. **Управление заявками** (назначение мастеров)
3. **Настройки системы**

### Фаза 4: Оптимизация (1 день)
1. **Кеширование данных**
2. **Lazy loading для больших таблиц**
3. **Real-time обновления**

## 🎯 Требуемые ресурсы

### От разработчика:
- ✅ Supabase URL и ключи доступа
- ✅ Подтверждение структуры БД
- ✅ n8n API credentials (опционально)

### От системы:
- ✅ Supabase проект с настроенной БД
- ✅ n8n с настроенными workflow (опционально)
- ✅ SSL сертификаты для безопасного подключения

## 📊 Метрики успеха

- **Производительность:** Загрузка данных < 2 сек
- **Надежность:** 99.9% uptime
- **Безопасность:** Все действия логируются
- **UX:** Интуитивный интерфейс для администраторов

## 🚀 Следующие шаги

1. **Получить доступы к Supabase**
2. **Проверить структуру БД**
3. **Реализовать базовые компоненты**
4. **Тестирование с реальными данными**

---

**Вопросы для уточнения:**
1. Какие конкретно метрики нужны в статистике?
2. Нужно ли real-time обновление данных?
3. Какие действия с пользователями/заявками требуются?
4. Есть ли специфические требования к экспорту данных?
