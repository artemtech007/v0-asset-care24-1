# Git & GitHub Workflow Guide - AssetCare24

**Версия:** 1.1
**Дата:** 20 января 2026 г.
**Автор:** AI Assistant

## ⚠️ ВАЖНО: Проблема с деплоем на Vercel (январь 2026)

**Проблема:** Vercel не реагировал на изменения в ветке `master` основного репозитория `v0-asset-care24-1`.

**Причина:** Vercel был настроен на автоматический деплой из ветки `main` подмодуля `site/`, а не из ветки `master` основного репозитория.

**Решение:**
1. Пушить frontend изменения в ветку `main` подмодуля `site/`:
   ```bash
   cd site/
   git checkout main
   git add .
   git commit -m "feat: Your changes"
   git push origin main
   ```
2. Обновлять ссылку на подмодуль в основном репозитории:
   ```bash
   cd ..  # вернуться в основной репозиторий
   git add site
   git commit -m "chore: Update site submodule"
   git push origin master
   ```

**Результат:** Деплой на Vercel происходит автоматически при пуше в `site/main`.

---

## 🎯 Обзор

Этот документ содержит полное руководство по работе с Git и GitHub для проекта AssetCare24. Здесь описаны все процессы, доступы и best practices для эффективной командной работы.

---

## 📋 Содержание

1. [Основные понятия Git](#-основные-понятия-git)
2. [Начальная настройка](#-начальная-настройка)
3. [Локальный запуск проекта](#-локальный-запуск-проекта)
4. [Ежедневный рабочий процесс](#-ежедневный-рабочий-процесс)
5. [Работа с ветками](#-работа-с-ветками)
6. [Разрешение конфликтов](#-разрешение-конфликтов)
7. [Структура проекта](#-структура-проекта)
8. [Деплой на Vercel](#-деплой-на-vercel)
9. [Доступы и аккаунты](#-доступы-и-аккаунты)
10. [Частые проблемы](#-частые-проблемы)
11. [Best Practices](#-best-practices)

---

## 🔧 Основные понятия Git

### Репозиторий (Repository)
- **Основной репозиторий:** `https://github.com/artemtech007/v0-asset-care24-1`
- **Продакшен версия:** `site/` (полное Next.js приложение для деплоя)
- **Общие компоненты:** `components/`, `lib/` (переиспользуемые компоненты)
- **Документация:** `mvp/` (папка с документацией и планами)
- **Рабочие файлы:** `docs/`, `infrastructure/`, `webhook/`, `twilio базов настр вебхука акк Антона`

### Коммит (Commit)
```bash
git commit -m "тип: краткое описание

- Детальное описание изменений
- Что именно изменилось
- Зачем эти изменения"
```

**Типы коммитов:**
- `feat:` - новая функциональность
- `fix:` - исправление багов
- `docs:` - изменения документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `chore:` - обслуживание (обновление зависимостей и т.д.)

### Ветки (Branches)
- `master/main` - основная ветка (production-ready код)
- `develop` - ветка разработки (опционально)
- `feature/*` - ветки новых функций
- `hotfix/*` - ветки исправлений

---

## ⚙️ Начальная настройка

### 1. Установка Git
```bash
# Ubuntu/Debian
sudo apt update && sudo apt install git

# macOS
brew install git

# Windows
# Скачать с https://git-scm.com/downloads
```

### 2. Настройка Git
```bash
# Глобальные настройки
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"

# Настройка редактора (опционально)
git config --global core.editor "nano"
# или
git config --global core.editor "code --wait"

# Проверка настроек
git config --list
```

### 3. Клонирование репозитория
```bash
# Клонировать основной репозиторий
git clone https://github.com/artemtech007/v0-asset-care24-1.git
cd v0-asset-care24-1

# Инициализировать подмодули
git submodule update --init --recursive
```

### 4. Настройка SSH (рекомендуется)
```bash
# Генерация SSH ключа
ssh-keygen -t ed25519 -C "ваш.email@example.com"

# Добавление ключа в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Копирование публичного ключа
cat ~/.ssh/id_ed25519.pub

# Добавление ключа в GitHub:
# 1. Зайти в Settings → SSH and GPG keys
# 2. New SSH key
# 3. Вставить содержимое ключа

# Использование SSH вместо HTTPS
git remote set-url origin git@github.com:artemtech007/v0-asset-care24-1.git
```

---

## 🚀 Ежедневный рабочий процесс

### Утро: Синхронизация с репозиторием
```bash
# Проверить статус
git status

# Получить последние изменения
git pull origin master

# Обновить подмодули
git submodule update --remote

# Проверить статус подмодулей
git status
```

### Работа над задачей

#### 1. Создание ветки для задачи
```bash
# Создать новую ветку
git checkout -b feature/new-feature-name

# Или для исправления
git checkout -b fix/bug-description
```

#### 2. Работа с кодом
- Вносить изменения в файлы
- Тестировать локально
- Проверять линтеры

#### 3. Коммит изменений
```bash
# Проверить что изменилось
git status

# Добавить файлы
git add .
# или конкретные файлы
git add site/components/new-component.tsx

# Создать коммит
git commit -m "feat: Add new registration form

- Split name field into Vorname/Nachname
- Rename telefon to whatsapp
- Add webhook integration
- Update validation logic"
```

### Вечер: Публикация изменений
```bash
# Для frontend изменений (site/) - деплоит на Vercel
cd site/
git checkout main
git add .
git commit -m "feat: Your frontend changes"
git push origin main
cd ..  # вернуться в основной репозиторий

# Обновить ссылку на подмодуль
git add site
git commit -m "chore: Update site submodule"
git push origin master

# Для backend/API изменений - создать PR
git push origin feature/new-feature-name
# Создать Pull Request через GitHub интерфейс
# После ревью и аппрува - merge в master
```

---

## 🌿 Работа с ветками

### Создание и переключение
```bash
# Создать и перейти на новую ветку
git checkout -b feature/new-feature

# Переключиться на существующую ветку
git checkout existing-branch

# Показать все ветки
git branch -a

# Удалить локальную ветку
git branch -d feature/completed-feature
```

### Синхронизация веток
```bash
# Получить все ветки из удаленного репозитория
git fetch origin

# Обновить текущую ветку
git pull origin master

# Отправить локальную ветку
git push origin feature/my-feature
```

### Merge vs Rebase
```bash
# Merge (сохраняет историю коммитов)
git checkout master
git merge feature/my-feature

# Rebase (чистая линейная история)
git checkout feature/my-feature
git rebase master
```

---

## ⚠️ Разрешение конфликтов

### При merge/rebase конфликте
```bash
# Проверить конфликтующие файлы
git status

# Открыть файлы с конфликтами в редакторе
# Найти маркеры конфликтов:
# <<<<<<< HEAD
# Ваш код
# =======
# Код из другой ветки
# >>>>>>> branch-name

# Разрешить конфликты вручную
# Затем добавить файлы
git add conflicted-file.tsx

# Завершить merge
git commit
# или для rebase
git rebase --continue
```

### Откат конфликта
```bash
# Отменить merge
git merge --abort

# Отменить rebase
git rebase --abort

# Вернуть файл к предыдущему состоянию
git checkout -- conflicted-file.tsx
```

---

## 🚀 Локальный запуск проекта

### Структура директорий после миграции
```
assetcare24/                   # Основной репозиторий
├── site/                      # ⭐ ПРОДАКШЕН ВЕРСИЯ (запускаем ОТСЮДА)
│   ├── app/                   # Next.js страницы
│   ├── components/            # UI компоненты
│   ├── lib/                   # Утилиты и API
│   └── package.json           # Зависимости
├── components/                # Общие компоненты (не для запуска)
├── lib/                       # Общие утилиты
├── mvp/                       # Документация
└── docs/                      # Дополнительная документация
```

### Правильный запуск проекта
```bash
# ❌ НЕПРАВИЛЬНО: запуск из корневой папки
cd /home/aaa/Projects/n8n/assetcare24
npm run dev  # Будет работать, но с устаревшими компонентами

# ✅ ПРАВИЛЬНО: запуск из папки site/
cd /home/aaa/Projects/n8n/assetcare24/site
npm run dev -- --port 3000

# Сайт будет доступен на http://localhost:3000
```

### Проверка запуска
```bash
# Убедитесь, что сайт работает:
curl http://localhost:3000
# Должен вернуть HTML с заголовком "AssetCare24"

# Проверьте отсутствие старых элементов:
curl http://localhost:3000 | grep -c "FloatingIcon"
# Должно быть 0 (FloatingIcon удалены)
```

### Для разработки компонентов
```bash
# Если разрабатываете общие компоненты в /components:
cd /home/aaa/Projects/n8n/assetcare24/components

# Если разрабатываете продакшен версию в /site:
cd /home/aaa/Projects/n8n/assetcare24/site
npm run dev -- --port 3000
```

---

## 📦 Структура проекта

### Архитектура после миграции
```
assetcare24/               # Единый репозиторий
├── site/                   # ⭐ ПРОДАКШЕН ПРИЛОЖЕНИЕ
│   ├── app/                # Next.js маршруты (17 страниц)
│   │   ├── /                # Главная страница
│   │   ├── /registrierung  # Регистрация (мастер + фирма)
│   │   ├── /firma-registrieren  # Регистрация фирмы
│   │   └── /dashboard/*     # Дашборды
│   ├── components/         # UI компоненты (88 файлов)
│   ├── lib/                # Утилиты (source-codes.ts)
│   └── package.json        # Версия 0.1.0
├── components/             # Общие компоненты (33 файла)
├── lib/                    # Общие утилиты
├── mvp/                    # Документация + планы
├── docs/                   # Техническая документация
└── infrastructure/         # Инфраструктурные файлы
```

### Git workflow после миграции
```bash
# Все изменения в site/ теперь фиксируются в основном репозитории
# Папка site/ - это просто директория в основном репозитории

# Стандартный workflow:
cd /home/aaa/Projects/n8n/assetcare24
git add .
git commit -m "feat: Add new functionality"
git push origin master

# Vercel автоматически задеплоит изменения
```

---

## 🚀 Деплой на Vercel

### Автоматический деплой
- **Триггер:** Push в ветку `master` основного репозитория
- **Репозиторий:** `https://github.com/artemtech007/v0-asset-care24-1.git`
- **Директория деплоя:** `site/` (продакшен версия)
- **URL:** `https://v0-asset-care24.vercel.app`
- **Время:** 2-5 минут после push

### Ручной redeploy
```bash
# Через Vercel CLI (если установлен)
vercel --prod

# Через Vercel dashboard:
# 1. Зайти в проект
# 2. Deployments → Trigger Deploy
# 3. Выбрать ветку main
```

### Проверка статуса deployment
```bash
# Проверить локально
cd site && npm run build

# Проверить Vercel dashboard
# https://vercel.com/artemtech007/v0-asset-care24-1
```

---

## 🔐 Доступы и аккаунты

### GitHub
- **Организация:** artemtech007
- **Репозиторий:** v0-asset-care24-1
- **Доступ:** Owner - Artem Tihonov
- **URL:** https://github.com/artemtech007/v0-asset-care24-1

### Vercel
- **Аккаунт:** artemtech007@gmail.com
- **Проект:** v0-asset-care24-1
- **Dashboard:** https://vercel.com/artemtech007/v0-asset-care24-1
- **Production URL:** https://v0-assetcare24.vercel.app

### Supabase (будущие доступы)
- **URL:** `https://[project-id].supabase.co`
- **Анонимный ключ:** Для клиентских запросов
- **Service Role ключ:** Для админских операций

### n8n
- **URL:** https://botstroicom.site
- **Аккаунт:** info@assetcare24.de
- **Пароль:** Tgbnhf6!+98+!

### Twilio
- **Аккаунт:** Настроен Антоном
- **Sandbox:** +14155238886
- **Production:** +14155238886

---

## 🐛 Частые проблемы

### 1. "Permission denied" при push
```bash
# Проверить SSH ключ
ssh -T git@github.com

# Использовать HTTPS вместо SSH
git remote set-url origin https://github.com/artemtech007/v0-asset-care24-1.git
```

### 2. Подмодуль не обновляется
```bash
# Принудительное обновление
git submodule update --init --force --remote

# Или переинициализация
rm -rf site
git submodule update --init --recursive
```

### 3. "Dirty submodule" ошибка
```bash
# Проверить статус подмодуля
cd site && git status

# Зафиксировать изменения в подмодуле
git add . && git commit -m "fix: Update submodule"
git push origin main

# Обновить в основном репозитории
cd .. && git add site && git commit -m "chore: Update submodule"
```

### 4. Vercel не деплойт
```bash
# Проверить build локально
cd site && npm run build

# Проверить логи в Vercel dashboard
# Или проверить .vercelignore файл
```

### 5. Git игнорирует файлы
```bash
# Проверить .gitignore
cat .gitignore

# Принудительно добавить файл
git add -f ignored-file.txt
```

---

## 📋 Best Practices

### 1. Коммиты
```bash
# Хороший коммит
git commit -m "feat: Add user authentication

- Implement JWT token validation
- Add login/logout endpoints
- Update user model with auth fields
- Add password hashing with bcrypt"

# Плохой коммит
git commit -m "update code"
```

### 2. Ветки
- Всегда работайте в отдельных ветках
- Регулярно синхронизируйтесь с master
- Удаляйте merged ветки

### 3. Pull Requests
- Создавайте PR для всех изменений
- Добавляйте описание и скриншоты
- Запрашивайте ревью перед merge

### 4. Документация
- Обновляйте документацию вместе с кодом
- Используйте понятные названия файлов
- Добавляйте примеры использования

### 5. Безопасность
- Никогда не коммитьте пароли/ключи
- Используйте .env файлы для секретов
- Проверяйте .gitignore перед коммитом

---

## 📞 Контакты

- **Разработчик:** Artem Tihonov (artemtech007@gmail.com)
- **Проект:** AssetCare24 MVP
- **Документация:** Всегда актуализируется в `mvp/` папке

---

## 🔄 Краткая шпаргалка

```bash
# Начало работы
git clone https://github.com/artemtech007/v0-asset-care24-1.git
cd v0-asset-care24-1
git submodule update --init --recursive

# Ежедневная работа
git status
git pull origin master
git checkout -b feature/my-feature
# ... работа ...

# Для frontend изменений:
cd site/
git checkout main
git add .
git commit -m "feat: Frontend changes"
git push origin main
cd ..
git add site
git commit -m "chore: Update site submodule"
git push origin master

# Для backend изменений:
git push origin feature/my-feature
# Создать PR в GitHub

# Работа с подмодулем
cd site
git add . && git commit -m "Update site"
git push origin main
cd ..
git add site && git commit -m "chore: Update site"
git push origin master
```

---

**Помните:** Всегда проверяйте `git status` перед коммитом и создавайте осмысленные сообщения коммитов!
