# 🕵️‍♂️ Анализ инцидента с деплоем Vercel и план рефакторинга

**Дата инцидента:** 05.02.2026
**Статус:** ✅ Решено (Версия 2.71 успешно развернута)

---

## 🚨 Разбор инцидента (Post-Mortem)

### **Симптомы**
1. Изменения успешно пушились в GitHub (ветки `master` и `main`).
2. Vercel показывал успешные деплои.
3. На публичном сайте отображалась устаревшая версия (2.01) вместо актуальной (2.71).
4. Проблема воспроизводилась как на старом, так и на новом проекте в Vercel.

### **Причины сбоя (Root Cause Analysis)**

Проблема возникла из-за комбинации трех факторов, создавших "идеальный шторм":

1.  **Нестандартная структура проекта:**
    *   Исходный код находился в подпапке `site/`, а не в корне.
    *   При создании нового проекта Vercel по умолчанию установил **Root Directory** в `.` (корень репозитория), где не было `package.json`.

2.  **"Призрак" Git Submodule:**
    *   Папка `site/` в какой-то момент имела признаки подмодуля (git submodule).
    *   Vercel выдавал предупреждение `Failed to fetch one or more git submodules`, что указывало на конфликт в индексах git, хотя локально папка выглядела обычной.

3.  **Рассинхронизация веток (Branch Confusion):**
    *   Активная разработка велась в ветке `master`.
    *   Vercel по умолчанию ожидал Production-деплой из ветки `main`.
    *   `master` воспринималась Vercel как "Preview" ветка, а `main` (которая отставала) считалась Production.

4.  **Отсутствие Environment Variables:**
    *   В новом проекте Vercel не были прописаны переменные `NEXT_PUBLIC_SUPABASE_URL` и ключи, что приводило к тихому падению билда или использованию кэша.

### **Как было исправлено**
1.  **Синхронизация:** Ветка `main` была принудительно обновлена до состояния `master`.
2.  **Конфигурация Vercel:**
    *   Установлен корректный **Root Directory**: `site`.
    *   Добавлены **Environment Variables** (Supabase ключи).
    *   Выполнен **Redeploy without Cache**.

---

## 🏗️ Рекомендации по Рефакторингу (Architecture & Workflow)

Чтобы исключить подобные проблемы в будущем и упростить поддержку проекта, рекомендуется привести структуру репозитория к стандартному "плоскому" виду.

### **1. Выпрямление структуры (Flattening)**

**Текущая структура (Проблемная):**
```
/ (root)
├── mvp/              # Документация, планы, SQL
├── site/             # Код Next.js приложения
│   ├── package.json
│   ├── app/
│   └── ...
└── ...
```

**Рекомендуемая структура (Целевая):**
```
/ (root)
├── package.json      # В КОРНЕ!
├── next.config.mjs   # В КОРНЕ!
├── .env.local        # В КОРНЕ!
├── app/              # Исходный код приложения
├── components/       # Компоненты
├── public/           # Статика
├── lib/              # Утилиты
├── docs/             # Бывшая папка mvp (только документация)
├── database/         # SQL миграции и скрипты (вынесены из mvp)
└── README.md         # Основной ридми
```

**Преимущества:**
*   **Zero Config Deployment:** Vercel, Netlify и другие платформы работают "из коробки", так как `package.json` находится там, где они ожидают.
*   **Упрощение Git:** Исключает риск создания вложенных подмодулей.
*   **Удобство IDE:** Проект открывается сразу с кодом, меньше вложенности.

### **2. Стандартизация веток (Git Flow)**

**Проблема:** Наличие и `master`, и `main` создает путаницу для CI/CD.

**Решение:**
1.  Выбрать **`main`** как единственную основную ветку (индустриальный стандарт).
2.  Полностью удалить ветку `master`.
3.  Настроить правило защиты ветки (Branch Protection) для `main`, чтобы предотвратить случайные force push.

### **3. Организация документации и ассетов**

Папка `mvp` сейчас перегружена разнородными файлами.

**Решение:** Разделить ответственность папок:
*   **`docs/`**: Только Markdown файлы с документацией.
*   **`database/`**: Все `.sql` файлы (миграции, сиды, схемы).
*   **`infrastructure/`**: Конфиги серверов, Nginx, Docker (если будет).
*   **`scripts/`**: Полезные bash/js скрипты для обслуживания.

### **4. План перехода (Migration Plan)**

1.  **Бэкап:** Создать резервную ветку `backup-before-refactor`.
2.  **Перемещение:**
    *   Перенести содержимое `site/*` в корень `./`.
    *   Переименовать `mvp` в `docs`.
    *   Создать папку `database` и перенести туда `.sql` из `docs`.
3.  **Чистка:** Удалить пустую папку `site`.
4.  **Git:**
    *   Закоммитить изменения.
    *   Переключиться на `main`.
    *   Удалить `master`.
5.  **CI/CD:**
    *   В Vercel изменить **Root Directory** обратно на `.` (или оставить пустым).

---

**Итог:** Этот рефакторинг займет около 30-60 минут, но сэкономит десятки часов на отладку инфраструктуры в будущем. Проект станет понятным для любого нового разработчика и совместимым с любыми инструментами деплоя по умолчанию.
