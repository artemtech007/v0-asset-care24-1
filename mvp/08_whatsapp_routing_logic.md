# Логика маршрутизации WhatsApp сообщений (AssetCare24 MVP)

**Дата:** 11 января 2026 г.
**Версия:** 1.0
**Статус:** Утверждено
**Ответственный:** AI Assistant

## 🎯 Обзор

Система маршрутизации WhatsApp сообщений с одним номером Twilio, использующая UTM-подобные метки и логику разделения по ролям/категориям пользователей.

## 🏗️ Архитектура разделения

### Принцип работы
1. **Все сообщения** → Один Twilio Webhook
2. **Анализ отправителя** → Проверка в БД users
3. **Определение роли/категории** → Маршрутизация в соответствующий workflow
4. **UTM-метки** → Для первичного определения категории клиентов

### Типы пользователей и их маршрутизация

#### 1. Работники (Masters)
**Идентификация:** `users.role = 'master'`
**Источник:** Всегда через веб-форму регистрации
**Маршрутизация:** В мастерский workflow
**Особенности:** Могут переключаться между ролями

#### 2. Клиенты (Clients)
**Подкатегории:**
- **Существующие клиенты компании:** По домам/комплексам
- **Новые клиенты:** С рекламы/сайта

---

## 🔄 UTM-подобные метки для WhatsApp

### Формат ссылок
```
https://wa.me/{номер}?text={UTM_метка}_{дополнительная_информация}
```

### Примеры меток

#### Для существующих клиентов (контрактные):
```
Дом №1: https://wa.me/14155238886?text=contract-1-qr-request
Комплекс A: https://wa.me/14155238886?text=contract-2-qr-request
```

#### Для новых клиентов (публичные):
```
Реклама 1 (Google/Facebook): https://wa.me/14155238886?text=public-1-ad1-request
Реклама 2 (Instagram/TikTok): https://wa.me/14155238886?text=public-2-ad2-request
Органический трафик: https://wa.me/14155238886?text=public-3-organic-request
```

### Логика обработки меток

#### В Router (Workflow 1):
```javascript
// Анализ первого сообщения от нового пользователя
function analyzeUTMTag(message) {
  if (message.startsWith("contract-")) {
    const parts = message.split("-");
    if (parts.length >= 3 && parts[3] === "request") {
      const number = parts[1];
      const source = parts[2];
      return {
        category: "existing_client",
        subcategory: `contract_${number}_${source}`,
        source: source === "qr" ? "qr_code" : source
      };
    }
  }

  if (message.startsWith("public-")) {
    const parts = message.split("-");
    if (parts.length >= 4 && parts[3] === "request") {
      const number = parts[1];
      const source = parts[2];
      let adSource = "unknown";

      if (source === "ad1") adSource = "google_facebook";
      else if (source === "ad2") adSource = "instagram_tiktok";
      else if (source === "organic") adSource = "website";

      return {
        category: "new_client",
        subcategory: `public_${number}_${source}`,
        source: adSource === "website" ? "organic" : "advertisement"
      };
    }
  }

  // Без метки - обычное сообщение
  return {
    category: "unknown_client",
    subcategory: null,
    source: "unknown"
  };
}
```

---

## 🔀 Логика маршрутизации в Router

### Workflow 1: Router (обновленная версия)

#### Шаг 1: Нормализация данных
- Приведение номера к формату +49...
- Извлечение текста и медиа

#### Шаг 2: Проверка пользователя
```sql
SELECT
  id, role, status,
  meta_data->>'client_category' as client_category,
  meta_data->>'current_mode' as current_mode
FROM users
WHERE phone = $normalized_phone
```

#### Шаг 3: Логика маршрутизации

##### Сценарий A: Новый пользователь (не найден в БД)
```javascript
// Анализ UTM-метки из первого сообщения
const utmData = analyzeUTMTag(message);

// Создание пользователя
const userData = {
  phone: normalizedPhone,
  role: 'client',  // По умолчанию клиент
  status: 'new',
  meta_data: {
    client_category: utmData.category,
    client_subcategory: utmData.subcategory,
    source: utmData.source,
    first_contact: new Date().toISOString()
  }
};

// Сохранение в БД
await createUser(userData);

// Переход к обработке заявки
executeWorkflow('Process_Request', {
  user_id: userData.id,
  category: utmData.category,
  subcategory: utmData.subcategory
});
```

##### Сценарий B: Существующий пользователь - Клиент
```javascript
if (user.role === 'client' || user.current_mode === 'client') {
  // Определение категории из meta_data или по умолчанию
  const category = user.client_category || 'unknown_client';

  // Маршрутизация по категории
  switch(category) {
    case 'existing_client':
      executeWorkflow('Process_Existing_Client_Request');
      break;
    case 'new_client':
      executeWorkflow('Process_New_Client_Request');
      break;
    default:
      executeWorkflow('Process_Unknown_Client_Request');
  }
}
```

##### Сценарий C: Существующий пользователь - Работник
```javascript
if (user.role === 'master' || user.current_mode === 'master') {
  // Все работники в один workflow
  executeWorkflow('Master_Router');
}
```

##### Сценарий D: Пользователь с двойной ролью
```javascript
// Если у пользователя есть и клиентская и мастерская роль
if (user.roles?.includes('client') && user.roles?.includes('master')) {
  // Спросить о режиме
  if (!user.current_mode) {
    sendWhatsAppMessage(
      phone,
      "Вы хотите связаться как клиент или как мастер? (ответьте 'клиент' или 'мастер')"
    );
    // Установить состояние ожидания выбора режима
    updateBotState(phone, 'waiting_for_mode_selection');
    return;
  }
}
```

---

## 📋 Категории клиентов

### Existing Clients (существующие клиенты компании)
- **contract-1-qr, contract-2-qr, ...** - Контрактные клиенты по QR-кодам
- **Особая обработка:** Приоритетные условия, известные адреса

### New Clients (новые клиенты)
- **public-1-ad1, public-2-ad2** - Клиенты с рекламы
- **public-3-organic** - Органический трафик с сайта
- **Обработка:** Дополнительные вопросы, стандартные условия

### Unknown Clients (неопределенные)
- **Обработка:** Запрос уточнения, определение категории

---

## 🔄 Переключение ролей

### Сценарий: Клиент хочет стать мастером
1. **Подача заявки** через форму регистрации мастера
2. **Проверка:** Номер телефона уже существует в БД
3. **Действия:**
   - Добавить роль 'master' к существующему пользователю
   - `meta_data.roles = ['client', 'master']`
   - `current_mode = 'master'` (по умолчанию новая роль активна)
   - Сохранить историю заявок как клиента

### Сценарий: Мастер пишет как клиент
- **В Router:** Если мастер пишет нестандартное сообщение
- **Вопрос:** "Вы пишете как мастер или как клиент?"
- **Сохранение:** `current_mode = выбранный_режим`

---

## 🗄️ Архитектура базы данных для множественных ролей

### Вариант 1: Простое переключение (для MVP)
```sql
-- Текущая структура с расширениями
ALTER TABLE users ADD COLUMN current_mode text; -- 'client' или 'master'
ALTER TABLE users ADD COLUMN roles jsonb DEFAULT '[]'::jsonb; -- ["client", "master"]

-- Пример данных
{
  "role": "client",           -- Основная роль для обратной совместимости
  "current_mode": "master",   -- Текущий активный режим
  "roles": ["client", "master"] -- Все доступные роли
}
```

### Вариант 2: Связанная таблица (для будущего)
```sql
CREATE TABLE user_roles (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  role text NOT NULL, -- 'client', 'master', 'admin'
  is_active boolean DEFAULT false,
  assigned_at timestamptz DEFAULT now(),
  assigned_by uuid REFERENCES users(id) -- Кто назначил роль
);

-- Пример: Один пользователь - и клиент и мастер
-- user_roles:
-- user_id: 123, role: 'client', is_active: false
-- user_id: 123, role: 'master', is_active: true
```

### Рекомендация для MVP
**Вариант 1** - достаточно для начала, легко расширить до Варианта 2 позже.

---

## 📊 Мониторинг и аналитика

### Метрики маршрутизации
- Количество сообщений по категориям
- Конверсия по источникам (UTM-метки)
- Время обработки маршрутизации
- Ошибки маршрутизации

### Алерты
- Новые категории клиентов
- Ошибки определения роли
- Долгое время без категории

---

## 🚀 Следующие шаги

1. **Реализовать UTM-анализ** в Router workflow
2. **Создать категории workflow** для разных типов клиентов
3. **Добавить логику переключения ролей**
4. **Сгенерировать QR-коды** с метками
5. **Тестировать маршрутизацию**

---

## ❓ Открытые вопросы

1. **Количество категорий:** Сколько домов/комплексов нужно поддержать?
2. **UI переключения:** Где пользователь сможет менять current_mode?
3. **История ролей:** Нужно ли логировать изменения ролей?
4. **Валидация UTM:** Что делать с невалидными метками?
