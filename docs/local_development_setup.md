# Локальная разработка AssetCare24

## 🚀 Запуск сайта

### ✅ Правильный способ
```bash
cd /home/aaa/Projects/n8n/assetcare24/site
npm run dev
```
- URL: `http://localhost:3000`

### ❌ Неправильные способы
```bash
# НЕПРАВИЛЬНО - устаревшая версия
cd /home/aaa/Projects/n8n/assetcare24
npm run dev

# НЕПРАВИЛЬНО - нет package.json
cd /home/aaa/Projects/n8n/assetcare24/app
npm run dev
```

## 📁 Структура проекта
```
/home/aaa/Projects/n8n/assetcare24/
├── site/           # ✅ АКТУАЛЬНЫЙ сайт Next.js
├── app/            # ❌ Старая версия (не использовать)
├── mvp/            # Документация
└── docs/           # Руководства
```

## 🛠️ Установка зависимостей
```bash
cd site
npm install  # или pnpm install
```

## 🔍 Проверка запуска
```bash
# Должен показать Next.js 16+ и localhost:3000
curl -s http://localhost:3000 | grep -o "<title>[^<]*"
# Ожидаемый ответ: AssetCare24 – Ihr intelligenter...
```

## 📝 Добавление новых страниц
- Создавать в `/site/app/[page-name]/page.tsx`
- Перезапуск сервера не требуется (горячая перезагрузка)
- Пример: UTM-тест доступен по `http://localhost:3000/utm-test`

## 🐛 Проблемы
- Если порт занят: `pkill -f "next dev"`
- Если зависимости не установлены: `rm -rf node_modules && npm install`
