# Архитектура S3-хранилища AssetCare24

**Дата:** 2025-12-12
**Версия:** 1.0
**Цель:** Организация хранения файлов для прототипа и будущих этапов
**Провайдер:** AWS S3 (или совместимый: DigitalOcean Spaces, MinIO)

---

## ОБЗОР АРХИТЕКТУРЫ

### Основные принципы
- **Иерархическая структура:** `bucket/folder/subfolder/filename`
- **UUID в названиях:** Для уникальности и безопасности
- **CDN интеграция:** CloudFront для быстрой доставки
- **Резервное копирование:** Cross-region replication
- **Доступ по ролям:** Разные права для разных типов пользователей

### Бакеты для прототипа (3 основных)

#### 1. assetcare24-uploads (Основной бакет для пользовательского контента)
**Назначение:** Файлы, загружаемые пользователями
**Регион:** eu-central-1 (Frankfurt)
**Класс хранения:** Standard
**CDN:** CloudFront distribution

#### 2. assetcare24-system (Системные файлы)
**Назначение:** Логи, бэкапы, системные файлы
**Регион:** eu-central-1 (Frankfurt)
**Класс хранения:** Standard-IA (для логов)
**Доступ:** Только администраторы

#### 3. assetcare24-backup (Резервные копии)
**Назначение:** Автоматические бэкапы базы данных и файлов
**Регион:** eu-west-1 (Ireland) - cross-region
**Класс хранения:** Glacier
**Доступ:** Только для восстановления

---

## СТРУКТУРА ОСНОВНОГО БАКЕТА (assetcare24-uploads)

```
/ (root)
├── users/                          # Профили пользователей
│   ├── avatars/                    # Аватары пользователей
│   │   ├── {user_uuid}/
│   │   │   ├── profile.jpg
│   │   │   └── thumbnail.jpg
│   └── portfolios/                 # Портфолио специалистов
│       ├── {specialist_uuid}/
│       │   ├── work1.jpg
│       │   ├── work2.jpg
│       │   └── before_after.zip
├── requests/                       # Заявки и работы
│   ├── photos/                     # Фото заявок
│   │   ├── {request_uuid}/
│   │   │   ├── problem1.jpg
│   │   │   ├── problem2.jpg
│   │   │   └── result.jpg
│   ├── videos/                     # Видео заявок (ОТДЕЛЬНЫЙ РАЗДЕЛ)
│   │   ├── {request_uuid}/
│   │   │   ├── problem_video.mp4
│   │   │   ├── progress_video.mp4
│   │   │   └── result_video.mp4
│   ├── documents/                  # Документы заявок
│   │   ├── {request_uuid}/
│   │   │   ├── invoice.pdf
│   │   │   └── contract.pdf
│   └── chats/                      # Файлы из чатов
│       ├── {request_uuid}/
│       │   ├── audio_message.mp3
│       │   ├── chat_video.mp4      # Видео из чатов
│       │   └── shared_file.pdf
├── properties/                     # Объекты недвижимости
│   └── photos/                     # Фото объектов
│       ├── {property_uuid}/
│       │   ├── facade.jpg
│       │   ├── entrance.jpg
│       │   └── common_areas.zip
└── temp/                           # Временные файлы
    ├── uploads/                    # Загрузки в процессе
    └── processing/                 # Файлы в обработке
```

---

## ПОДРОБНАЯ СТРУКТУРА ПО ТИПАМ ФАЙЛОВ

### 1. Аватары пользователей (users/avatars/)
```
users/avatars/{user_uuid}/
├── profile.{ext}          # Основное фото (max 2MB)
├── thumbnail.{ext}        # Миниатюра 150x150 (auto-generated)
└── original.{ext}         # Оригинал для бэкапа
```

**Ограничения:**
- Размер: до 2MB
- Форматы: JPG, PNG, WebP
- Разрешение: min 200x200, max 2000x2000

### 2. Фото заявок (requests/photos/)
```
requests/photos/{request_uuid}/
├── problem_{timestamp}.{ext}     # Фото проблемы
├── progress_{timestamp}.{ext}    # Фото в процессе
├── result_{timestamp}.{ext}      # Фото результата
└── metadata.json                 # EXIF и другая информация
```

**Ограничения:**
- Размер файла: до 10MB на фото
- Количество: до 10 фото на заявку
- Форматы: JPG, PNG, WebP
- Автоматическое сжатие и оптимизация

### 2.1. Видео заявок (requests/videos/) - ОТДЕЛЬНЫЙ РАЗДЕЛ
```
requests/videos/{request_uuid}/
├── problem_{timestamp}.{ext}     # Видео проблемы
├── progress_{timestamp}.{ext}    # Видео процесса работ
├── result_{timestamp}.{ext}      # Видео результата
├── thumbnails/                   # Миниатюры видео
│   ├── problem_thumb.jpg
│   └── result_thumb.jpg
└── metadata.json                 # Длительность, разрешение, кодек
```

**Ограничения:**
- Размер файла: до 100MB на видео
- Длительность: до 3 минут
- Количество: до 3 видео на заявку
- Форматы: MP4, MOV, WebM
- Автоматическое транскодирование в HLS для streaming
- Создание миниатюр (thumbnail) автоматически

### 3. Портфолио специалистов (users/portfolios/)
```
users/portfolios/{specialist_uuid}/
├── photos/                        # Фото работ
│   └── {work_uuid}/
│       ├── before.jpg             # Фото до работы
│       ├── after.jpg              # Фото после работы
│       └── description.json       # Описание работы
├── videos/                        # Видео работ (ОТДЕЛЬНЫЙ ПОДРАЗДЕЛ)
│   └── {work_uuid}/
│       ├── showcase.mp4           # Демонстрационное видео
│       ├── process.mp4            # Видео процесса
│       ├── thumbnail.jpg          # Миниатюра видео
│       └── metadata.json          # Информация о видео
└── certifications/                # Сертификаты и документы
    ├── certificate1.pdf
    └── license.pdf
```

**Ограничения:**
- Фото: до 5MB, до 20 работ на специалиста
- Видео: до 50MB, до 2 минут, до 5 видео на специалиста
- Документы: до 10MB PDF
- HLS транскодирование для видео в портфолио

### 4. Документы заявок (requests/documents/)
```
requests/documents/{request_uuid}/
├── invoices/
│   ├── invoice_{number}.pdf
│   └── receipt_{number}.pdf
├── contracts/
│   └── contract_{request_uuid}.pdf
└── reports/
    ├── work_report.pdf
    └── quality_check.pdf
```

### 5. Чат файлы (requests/chats/)
```
requests/chats/{request_uuid}/
├── {message_uuid}/
│   ├── audio_{timestamp}.mp3    # Голосовые сообщения
│   ├── image_{timestamp}.jpg    # Фото из чата
│   ├── video_{timestamp}.mp4    # Видео из чата
│   └── file_{timestamp}.{ext}   # Другие файлы
├── videos/                      # Видео файлы из чатов (ОТДЕЛЬНЫЙ ПОДРАЗДЕЛ)
│   ├── {message_uuid}/
│   │   ├── video.mp4
│   │   ├── thumbnail.jpg        # Автоматическая миниатюра
│   │   └── metadata.json        # Информация о видео
└── attachments/
    └── shared_{timestamp}.{ext} # Общие файлы
```

---

## ПРАВА ДОСТУПА И БЕЗОПАСНОСТЬ

### Политики доступа (IAM Policies)

#### Для пользователей (через API):
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "s3:GetObject",
        "s3:PutObject",
        "s3:DeleteObject"
      ],
      "Resource": [
        "arn:aws:s3:::assetcare24-uploads/users/${aws:userid}/*",
        "arn:aws:s3:::assetcare24-uploads/requests/${request_id}/*"
      ]
    }
  ]
}
```

#### Для специалистов:
- Доступ к своему портфолио
- Доступ к фото заявок, где они назначены
- Загрузка фото результатов работ

#### Для клиентов:
- Доступ к фото своих заявок
- Загрузка фото проблем
- Просмотр результатов работ

### Public доступ (через CloudFront):
- Аватары пользователей: публичный
- Фото заявок: публичный (после модерации)
- Портфолио специалистов: публичный

### Защищенные файлы:
- Счета и договоры: доступ только участникам
- Личные документы: шифрование на уровне объекта

---

## CDN И ДОСТАВКА КОНТЕНТА

### CloudFront Distribution
**Основная дистрибуция:**
- Origin: assetcare24-uploads
- Behaviors:
  - `/users/avatars/*` → Cache 1 hour
  - `/requests/photos/*` → Cache 24 hours
  - `/users/portfolios/*` → Cache 6 hours

**Дополнительные оптимизации:**
- WebP конвертация для изображений
- Responsive images (srcset)
- Lazy loading поддержка

### Кеширование
```
Cache Behaviors:
├── Static assets (avatars)     → TTL: 3600s
├── Request photos              → TTL: 86400s
├── Request videos              → TTL: 604800s (7 дней)
├── Portfolio images            → TTL: 21600s
├── Portfolio videos            → TTL: 604800s (7 дней)
├── Documents                   → No cache (private)
├── Chat files (photos)         → TTL: 1800s
└── Chat videos                 → TTL: 3600s
```

### Видео-специфичные настройки
- **HLS Streaming:** Видео конвертируются в HLS формат для progressive streaming
- **Adaptive Bitrate:** Создание нескольких качеств (360p, 720p, 1080p)
- **Preload:** Миниатюры видео кешируются отдельно
- **Range Requests:** Поддержка seek в видео

---

## ОБРАБОТКА И ОПТИМИЗАЦИЯ ФАЙЛОВ

### Lambda@Edge функции

#### 1. Image Optimization
```javascript
// Автоматическая оптимизация изображений
exports.handler = async (event) => {
  const request = event.Records[0].cf.request;
  const uri = request.uri;

  // WebP конвертация
  if (uri.match(/\.(jpg|jpeg|png)$/)) {
    request.uri = uri.replace(/\.(jpg|jpeg|png)$/, '.webp');
  }

  return request;
};
```

#### 2. Thumbnail Generation
```javascript
// Создание миниатюр для фото и видео
const AWS = require('aws-sdk');
const sharp = require('sharp');

exports.handler = async (event) => {
  const s3 = new AWS.S3();
  const bucket = event.Records[0].s3.bucket.name;
  const key = event.Records[0].s3.object.key;

  // Миниатюры для аватаров
  if (key.includes('/avatars/') && !key.includes('/thumbnail')) {
    const image = await s3.getObject({ Bucket: bucket, Key: key }).promise();
    const thumbnail = await sharp(image.Body)
      .resize(150, 150, { fit: 'cover' })
      .jpeg({ quality: 80 })
      .toBuffer();

    await s3.putObject({
      Bucket: bucket,
      Key: key.replace(/\/([^\/]+)$/, '/thumbnail_$1'),
      Body: thumbnail,
      ContentType: 'image/jpeg'
    }).promise();
  }

  // Миниатюры для видео (первый кадр)
  if (key.includes('/videos/') && key.match(/\.(mp4|mov|webm)$/)) {
    // Используем MediaConvert или ffmpeg для извлечения первого кадра
    // Пример с использованием external service или Lambda layer
    const thumbnailKey = key.replace(/\.(mp4|mov|webm)$/, '_thumb.jpg');
    // ... код для создания миниатюры из видео
  }
};
```

#### 3. Video Processing (НОВЫЙ)
```javascript
// Транскодирование видео в HLS формат
const AWS = require('aws-sdk');

exports.handler = async (event) => {
  const mediaConvert = new AWS.MediaConvert();
  const s3 = new AWS.S3();

  const bucket = event.Records[0].s3.bucket.name;
  const key = event.Records[0].s3.object.key;

  // Только для видео файлов
  if (key.includes('/videos/') && key.match(/\.(mp4|mov|webm)$/)) {
    const jobParams = {
      Role: process.env.MEDIA_CONVERT_ROLE,
      Settings: {
        Inputs: [{
          FileInput: `s3://${bucket}/${key}`,
          VideoSelector: {},
          AudioSelectors: { 'Audio Selector 1': { DefaultSelection: 'DEFAULT' } }
        }],
        OutputGroups: [{
          Name: 'HLS Group',
          OutputGroupSettings: {
            HlsGroupSettings: {
              SegmentLength: 10,
              MinSegmentLength: 0,
              Destination: `s3://${bucket}/${key.replace(/\.[^/.]+$/, '')}/`
            }
          },
          Outputs: [
            { NameModifier: '_360p', VideoDescription: { Width: 640, Height: 360 } },
            { NameModifier: '_720p', VideoDescription: { Width: 1280, Height: 720 } }
          ]
        }]
      }
    };

    await mediaConvert.createJob(jobParams).promise();
  }
};
```

### Метаданные и поиск

#### Для изображений (metadata.json)
```json
{
  "original_name": "problem_photo.jpg",
  "size": 2457600,
  "dimensions": { "width": 1920, "height": 1080 },
  "format": "JPEG",
  "uploaded_by": "user_uuid",
  "uploaded_at": "2025-12-12T10:30:00Z",
  "request_id": "request_uuid",
  "type": "photo",
  "tags": ["problem", "kitchen", "leak"],
  "exif": {
    "camera": "iPhone 12",
    "timestamp": "2025-12-12T10:25:00Z",
    "gps": { "lat": 52.5200, "lng": 13.4050 }
  }
}
```

#### Для видео (metadata.json)
```json
{
  "original_name": "problem_video.mp4",
  "size": 52428800,
  "duration": 45.67,
  "dimensions": { "width": 1920, "height": 1080 },
  "format": "MP4",
  "codec": "H.264",
  "bitrate": 5000000,
  "uploaded_by": "user_uuid",
  "uploaded_at": "2025-12-12T10:30:00Z",
  "request_id": "request_uuid",
  "type": "video",
  "hls_available": true,
  "qualities": ["360p", "720p", "1080p"],
  "thumbnail_url": "https://cdn.assetcare24.org/.../thumbnail.jpg",
  "tags": ["problem", "plumbing", "leak"]
}
```

---

## РЕЗЕРВНОЕ КОПИРОВАНИЕ И ВОССТАНОВЛЕНИЕ

### Стратегия бэкапа

#### Ежедневный бэкап:
- База данных: PostgreSQL dump → S3 backup bucket
- Критические файлы: Cross-region replication
- Метаданные: JSON export всех объектов

#### Резервное копирование:
```bash
# PostgreSQL бэкап
pg_dump -h $DB_HOST -U $DB_USER -d $DB_NAME \
  | gzip > backup_$(date +%Y%m%d_%H%M%S).sql.gz

# Загрузка в S3
aws s3 cp backup.sql.gz s3://assetcare24-backup/database/

# Файлы (только метаданные, сами файлы уже реплицированы)
aws s3 sync s3://assetcare24-uploads/ s3://assetcare24-backup/files/ \
  --exclude "*" --include "*.json" --include "*.log"
```

### Восстановление
1. **База данных:** `pg_restore` из последнего бэкапа
2. **Файлы:** Репликация из backup bucket
3. **Проверка целостности:** Сравнение checksums

---

## МОНИТОРИНГ И АНАЛИТИКА

### Метрики CloudWatch
- **Размер хранилища:** По бакетам
- **Количество объектов:** По типам
- **Трафик:** Загрузки/скачивания
- **Ошибки:** 4xx/5xx ответы

### Стоимость оптимизации
- **Storage Class Analysis:** Автоматический переход в IA/Glacier
- **Lifecycle Policies:** Удаление временных файлов
- **Compression:** Gzip для текстовых файлов

---

## РАСШИРЕНИЕ ДЛЯ БУДУЩИХ ЭТАПОВ

### Этап 2: Дополнительные бакеты
- `assetcare24-media`: Видео контент (теперь частично реализовано в videos/)
- `assetcare24-documents`: Архивные документы
- `assetcare24-backup-media`: Резервные копии видео

### Этап 3: Продвинутые возможности
- **AI обработка изображений:** Автоматическая категоризация фото и видео
- **OCR:** Распознавание текста в документах
- **Video streaming:** HLS/DASH для видео (частично реализовано)
- **Video AI:** Автоматическое описание видео, распознавание объектов
- **Global CDN:** Multi-region доставка
- **Video compression:** HEVC/H.265 для лучшего сжатия

---

## ПОЧЕМУ ВИДЕО В ОТДЕЛЬНЫХ РАЗДЕЛАХ?

### Технические причины:
1. **Размер файлов:** Видео 10-100MB+ vs фото 1-10MB
2. **Обработка:** Требуют транскодирования, создания HLS
3. **Доставка:** Streaming vs простая загрузка
4. **Хранение:** Разные классы хранения и lifecycle

### Операционные преимущества:
1. **Мониторинг:** Отдельная статистика по видео контенту
2. **Кеширование:** Разные стратегии для видео/фото
3. **Резервное копирование:** Специфические правила для больших файлов
4. **Масштабирование:** Легче оптимизировать каждый тип контента

### Стоимостные аспекты:
- Видео могут использовать Glacier раньше
- Разные CDN тарифы для видео
- Оптимизация под конкретные паттерны использования

---

## ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ

### SDK и интеграции
```javascript
// Пример загрузки файла
import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';

const s3Client = new S3Client({
  region: 'eu-central-1',
  credentials: { ... }
});

const uploadFile = async (file, path) => {
  const command = new PutObjectCommand({
    Bucket: 'assetcare24-uploads',
    Key: path,
    Body: file,
    ContentType: file.type,
    Metadata: {
      'uploaded-by': userId,
      'request-id': requestId
    }
  });

  return await s3Client.send(command);
};
```

### Безопасность
- **VPC Endpoints:** Для внутренних сервисов
- **Encryption:** SSE-KMS для чувствительных данных
- **Access Logging:** CloudTrail для аудита

---

**S3 архитектура готова к развертыванию!** 🚀

Эта структура обеспечит масштабируемое, безопасное и эффективное хранение файлов для прототипа и будущих этапов развития платформы.
